# Import secret (still needed for JWT)
spring.config.import=optional:classpath:application-secret.properties
 
# -------------------------------------
# HARDCODED URL AUTH (Updated Password)
# -------------------------------------
# We embed the new password 'lordanthonelord' directly.
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require&user=postgres.kitxtqufhhgosxtskxhy&password=lordanthonelord
 
# Leave these blank so Spring uses the URL credentials
spring.datasource.username=
spring.datasource.password=
 
# Driver
spring.datasource.driver-class-name=org.postgresql.Driver
 
# -------------------------------------
# HIKARI POOL
# -------------------------------------
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=30000
 
# -------------------------------------
# JPA
# -------------------------------------
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://kitxtqufhhgosxtskxhy.supabase.co/auth/v1
 
